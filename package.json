{
  "name": "demand-desk",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Daily luxury demand, supply and resale-value tracker with append-only snapshots and CSV exports.",
  "scripts": {
    "scrape": "node scripts/scrape.mjs",
    "export": "node scripts/export-csv.mjs",
    "derive": "node scripts/derive.mjs",
    "notify": "node scripts/notify.mjs",
    "digest": "node scripts/digest.mjs",
    "build": "node scripts/build-site.mjs",
    "bundle": "node scripts/bundle.mjs",
    "all": "npm run scrape && npm run export && npm run derive && npm run build && npm run bundle",
    "serve": "node scripts/serve.mjs"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  }
}
